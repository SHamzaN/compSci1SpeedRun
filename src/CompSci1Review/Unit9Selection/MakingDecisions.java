package CompSci1Review.Unit9Selection;

public class MakingDecisions
{

    /*
    Java if Structure
    This same structure is used to form if statements in Java. However, there is one significant difference between a Jeroo if structure and a Java if structure.
    The condition of a Jeroo if structure is limited to a call to one of the Jeroo sensor methods such as isNet, isWater, isFacing, hasFlower, etc.
     On the other hand, the condition of a Java if structure does not have this limitation therefore it can contain expressions that include literals, variables, methods, and relational operators.
     */

    /*
    Blocked Structure
If the body of an if structure contains more than one statement the statements must by enclosed in curly braces { }.
If the if structure contains only one statement the curly braces are optional.
     */

    /*
    if-else Statement
Like Jeroo, Java provides an if-else structure. An if-else is used when you want to do one thing if a condition is true and another thing if that condition is false.
An if-else will always execute either the if section or the else section, but never both.
     */
    public static void main(String[]args)
    {

//        if(age == 18)       // condition contains a variable and int literal
//            System.out.println("I can vote");
//
//        if(cost > moneyInPocket)     // condition contains two variables
//            System.out.println("I can't buy it");
//
//        if(answer.equals("A"))       // condition contains a method call
//            score++;
//
//        if(isVowel(letter))         // condition contains a method call
//            System.out.println("The letter is a vowel");

//        if(amount <= balance)
//        {
//            double newBalance = balance - amount;
//            balance = newBalance;
//        }

//        if(average == 100)
//        {
//            grade = "A+";
//        }

//        if(average == 100)
//            grade = "A+";

//        if(condition)
//        {
//            body1     // if section
//        }
//        else
//        {
//            body2     // else section
//        }

//        if(amount <= balance)
//        {
//            balance = balance - amount;
//        }
//        else
//        {
//            System.out.println("You don't have enough money");
//        }

//        if(amount <= balance)
//            balance = balance - amount;
//        else
//            System.out.println("You don't have enough money");

//        if(amount <= balance) {
//            balance = balance - amount;
//        }
//        else {
//            System.out.println("You don't have enough money");
//        }
    }


}
